const path = require('path')

const PREFIX = '#'
const BOT_EMOJI = '❤️'
const TEMP_FOULDER = path.resolve(__dirname, '..', 'assets', 'temp')

module.export = {
  BOT_EOJI,
  PREFIX,
  TEMP_FOULDER
  }